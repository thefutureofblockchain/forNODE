ok so wtfffffff bitcchhhhhg ejfje
thees is deefffictl
ok so i need a split functio
i can split the main input like

camel Case Sucks
and then i can lower all
and add
def uppers(b):
    if b.isupper():
        b = b.replace(b, "_" + b.lower())
        return b
if user_input.find(letter.isupper) == 1:
   uppers(letter)
else:
   uppers(letter)
   upper